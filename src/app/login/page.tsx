import { AuthButtonServer } from "@/components/common/Auth-button-server";

export default function Login() {
    return (
        <section>
            <AuthButtonServer />
        </section>
    )
}